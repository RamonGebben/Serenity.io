#! /usr/local/bin/node

var program = require('commander');
var info = require('../package.json');
program
  .version(info.version)
  .option('-a, --argument', 'Trigger arg')
  .parse(process.argv);
 
if (program.argument) console.log('You triggered argument');

